# StyLua's own defaults, stated explicitly so the choice survives a change of
# default. Tabs mean each reader picks their own width; indent_width only tells
# StyLua how wide to assume a tab is when deciding where to wrap.
indent_type = "Tabs"
indent_width = 2
