-- Lua files are formatted by StyLua, which indents with tabs. vim-sleuth
-- detects that and sets 'noexpandtab' with 'shiftwidth' at 0 (follow
-- 'tabstop'), so the display width is all that is left to choose.
vim.bo.tabstop = 2
