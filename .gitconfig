[user]
  name = Teemu Viikeri
  email = teemu.viikeri@haltu.fi
[init]
  defaultBranch = main
[core]
  editor = nvim
[alias]
  amendpush = "!f() { git add \"$1\" && git commit --amend --no-edit && git push -f; }; f"
  st = status
  sw = switch
